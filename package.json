{
  "name": "docs.helperiq.io",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "npm run build && wrangler pages deploy .vitepress/dist --project-name docs-helperiq-io --branch main"
  },
  "devDependencies": {
    "mermaid": "^11.4.1",
    "playwright": "^1.60.0",
    "vitepress": "^1.6.3",
    "vitepress-plugin-mermaid": "^2.0.17"
  }
}

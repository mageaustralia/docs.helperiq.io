# Configuration

::: warning Coming soon
Per-area configuration pages haven't been written yet. This is a TOC
for what's planned:

- `config.toml` reference (database, redis, SMTP defaults, log level)
- Email inbox setup (IMAP/SMTP, OAuth, aliases, auto-assign on reply)
- Live chat widget
- AI providers (OpenAI, Claude, OpenRouter — keys, models, default
  embedding model)
- Knowledge sources (webpages, macros, custom snippets, file uploads)
- Ecommerce integration (Maho / Magento 1 OAuth setup)
- Per-inbox AI overrides
- PCI scrubbing
- SLA policies
- Macros
- Automations

For now, see [Getting Started → Installation](../getting-started/installation)
for the bootstrap config, and the
[API Reference](../api-reference/) for the underlying endpoints.
:::
